// 형 변환(Type conversion)

const a = 1;
const b = '1';

console.log(a === b);
console.log(a == b);

// Truthy (참 같은 것)
// true, {}, [], 1, 2, 'false', -12, '3.14'...

// Falsy (거짓 같은 것)
// false, '', null, undifined, 0, -0, NaN

if (true) {
    console.log(123);
}